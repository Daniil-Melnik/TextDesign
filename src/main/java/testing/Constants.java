package testing;

import java.awt.*;

/**
 * Класс констант, используемых в работе приложения.
 * Содержит размеры окна, панелей и цветовые константы.
 */
public class Constants {

    /** Максимальная ширина контент-строки в пикселях. */
    public static final int MAX_TEXT_W = 750;

    /** Ширина главного окна приложения в пикселях. */
    public static final int FRAME_W = 800;

    /** Высота главного окна приложения в пикселях. */
    public static final int FRAME_H = 400;

    /** Ширина панели с контент-текстом и текстового компонента в пикселях. */
    public static final int TEXT_PANEL_W = 800;

    /** Высота панели с контент-текстом и текстового компонента в пикселях. */
    public static final int TEXT_PANEL_H = 250;

    /**
     * Бледно-розовый цвет для сигнализации о превышении допустимой длины строки.
     * Используется для подсветки поля ввода при переполнении.
     */
    public static final Color PALE_RED = new Color(255, 194, 194);
}